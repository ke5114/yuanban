package org.example.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MyBatisConfig {
    // 使用 Spring Boot 的自动配置
    // 所有 MyBatis 配置都在 application.properties 中
}